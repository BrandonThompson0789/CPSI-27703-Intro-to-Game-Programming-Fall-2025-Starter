Place your .wav (or compatible) sound effects in this directory. Update
`assets/soundData.json` with matching relative paths so the SoundManager can
load them at runtime. Filenames referenced in the default configuration are
placeholders and should be replaced with your own audio assets.

