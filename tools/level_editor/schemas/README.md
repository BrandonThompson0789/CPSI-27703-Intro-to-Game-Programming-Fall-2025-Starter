# Schema Metadata

Place auto-generated or hand-authored schema descriptor files here (for example, component metadata or validation rules). Keeping them under source control makes it easy for tooling to regenerate the schema map without parsing the entire codebase each time.

